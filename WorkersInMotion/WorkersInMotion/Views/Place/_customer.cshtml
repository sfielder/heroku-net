@model WorkersInMotion.Model.ViewModel.PeopleViewForCreate
@{
    //  Layout = "~/Views/Shared/MasterPage.cshtml";
    Layout = null;
}

@*<div class="col-md-4">
        <div class="portlet box grey">
            <div class="portlet-title">
                <div class="caption"><i class="icon-group"></i>Choose a User Below:</div>
            </div>
            <div class="portlet-body">
                <table class="table table-striped table-bordered table-hover ">
                    <tr>
                        <th>

                        </th>
                        <th>
                            Users
                        </th>
                    </tr>
                    @if (Model.PlaceViewModel.User != null && Model.PlaceViewModel.User.Count() > 0)
                    {
                        foreach (var item in Model.PlaceViewModel.User)
                        {
                            <tr class="odd gradeX">
                                <td>
                                    @Html.HiddenFor(modelitem => item.OrganizationGUID)
                                    @Html.HiddenFor(modelitem => item.UserGUID)
                                    @Html.RadioButton("rdoUser", item.UserGUID, new { @id = "rdo_btn_" + item.UserGUID })
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.FirstName)
                                    @Html.DisplayFor(modelItem => item.LastName)
                                </td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="2" align="center" style="color:red">
                                No Data Found
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>*@
<div class="col-md-12">
    <div class="portlet box grey">
        <div class="portlet-title">
            <div class="caption"><i class="icon-user"></i>Clients</div>
        </div>
        <div class="portlet-body">
            <div style="overflow-x:scroll">
                <table class="table table-striped table-bordered table-hover">
                    <tr>

                        <th></th>
                        <th>
                            Client
                        </th>
                        @*<th>
                                Address
                            </th>
                            <th>
                                Primary Contact
                            </th>
                            <th></th>*@
                    </tr>
                    @if (Model.PlaceViewModel.Place != null && Model.PlaceViewModel.Place.Count() > 0)
                    {
                        foreach (var item in Model.PlaceViewModel.Place)
                        {
                            <tr class="odd gradeX">

                                <td>
                                    @Html.RadioButton("rdoPlace", item.PlaceGUID, new { @id = "rdo_btn_place_" + item.PlaceGUID })
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.PlaceName)
                                    @Html.HiddenFor(modelItem => item.PlaceName, new { @id = "hdn_placename_" + item.PlaceGUID })
                                    @Html.HiddenFor(modelItem => item.UserGUID, new { @id = "hdn_userguid_" + item.PlaceGUID })
                                    @Html.HiddenFor(modelItem => item.OrganizationGUID, new { @id = "hdn_orgguid_" + item.PlaceGUID })
                                </td>
                                @*<td>

                                    @Html.DisplayFor(modelItem => item.AddressLine1)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.AddressLine2)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.City)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.State)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.Country)
                                </td>
                                <td>

                                    @Html.DisplayFor(modelItem => item.FirstName)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.PlacePhone)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.MobilePhone)
                                    <br />
                                    @Html.DisplayFor(modelItem => item.Emails)

                                </td>
                                <td>
                                    <div style="padding:5px">
                                        @Html.ActionLink("Stops", "Index", "Market", new { id = item.PlaceGUID }, new { @class = "label label-sm label-info", @style = "border-radius:0px;margin-right:2px;" })
                                    </div>
                                    <div style="padding:5px">
                                        @Html.ActionLink("Contacts", "Index", "People", new { id = item.PlaceGUID }, new { @class = "label label-sm label-default", @style = "border-radius:0px;" })
                                    </div>
                                </td>*@
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="2" align="center" style="color:red">
                                No Data Found
                            </td>
                        </tr>
                    }

                </table>
            </div>
            <div class="form-actions right">
                <button id="btncancelpicker" type="button" onclick="return pickerCancelClick();" class="btn default">Cancel</button>
                <button id="btncontinuepicker" type="button" onclick="return pickerContinueClick();" class="btn blue"><i class="icon-ok"></i> Continue</button>
            </div>
        </div>
    </div>
</div>
